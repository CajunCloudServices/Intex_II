name: Deploy Tanglaw Project

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: tanglaw-project-prod-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy on production runner
        run: /home/lajicpajam/bin/harborlight-deploy.sh
